// High Style Match — client approval + final delivery bridge for the photographer dashboard.
(function(){
  'use strict';
  const ROOT='/high-style-match/';
  let decorating=false;
  const state=()=>{try{return JSON.parse(localStorage.getItem('hsmPremium1')||'{}')}catch{return{}}};
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  function addStyles(){if(document.getElementById('hsmClientWorkflowCss'))return;const s=document.createElement('style');s.id='hsmClientWorkflowCss';s.textContent=`
  .hsm-client-flow{display:flex;gap:0;overflow-x:auto;margin:0 0 18px;border:1px solid #25282e;background:#0d0f12;scrollbar-width:none}.hsm-client-flow::-webkit-scrollbar{display:none}.hsm-flow-step{min-width:145px;flex:1;padding:11px 12px;border-right:1px solid #25282e}.hsm-flow-step:last-child{border-right:0}.hsm-flow-step i{display:block;color:#626872;font:700 8px/1 -apple-system,BlinkMacSystemFont,"SF Pro Text",sans-serif;letter-spacing:.12em;font-style:normal;margin-bottom:6px}.hsm-flow-step b{display:block;color:#9ba1aa;font:650 10px/1.25 -apple-system,BlinkMacSystemFont,"SF Pro Text",sans-serif;white-space:nowrap}.hsm-flow-step.on{background:#151712}.hsm-flow-step.on i,.hsm-flow-step.on b{color:#dfc78f}.hsm-flow-step.done i{color:#77bd8e}.hsm-flow-step.done b{color:#cbd0d6}
  .hsm-client-bridge{position:relative;overflow:hidden;margin:0 0 16px;border:1px solid rgba(223,199,143,.24);background:#0d0d0a;padding:20px;display:flex;justify-content:space-between;align-items:center;gap:24px}.hsm-client-bridge:after{content:"";position:absolute;width:260px;height:1px;right:-50px;top:32px;background:linear-gradient(90deg,transparent,rgba(223,199,143,.32),transparent);transform:rotate(-13deg);pointer-events:none}.hsm-client-bridge>div,.hsm-client-bridge .hsm-bridge-actions{position:relative;z-index:1}.hsm-client-bridge .kicker{font-size:8px;letter-spacing:.16em;text-transform:uppercase;color:#dfc78f;font-weight:800}.hsm-client-bridge h3{font-family:Georgia,"Times New Roman",serif;font-size:23px;font-weight:400;letter-spacing:-.03em;color:#f3efe6;margin:6px 0}.hsm-client-bridge p{margin:0;color:#92978f;font-size:11px;line-height:1.55;max-width:680px}.hsm-bridge-actions{display:flex;gap:8px;flex:0 0 auto}.hsm-bridge-btn{min-height:40px;border-radius:2px;padding:0 13px;border:1px solid #30332f;background:#141611;color:#e7e4db;font:750 10px -apple-system,BlinkMacSystemFont,"SF Pro Text",sans-serif;cursor:pointer}.hsm-bridge-btn.primary{background:#dfc78f;border-color:#dfc78f;color:#11100c}.hsm-bridge-btn:hover{transform:translateY(-1px)}
  .hsm-approved-modal{position:fixed;inset:0;z-index:100000;background:rgba(0,0,0,.72);backdrop-filter:blur(8px);display:grid;place-items:center;padding:18px}.hsm-approved-card{width:min(760px,100%);max-height:min(78vh,760px);overflow:auto;background:#0d0d0a;border:1px solid rgba(223,199,143,.25);padding:20px;color:#f3efe6}.hsm-approved-head{display:flex;justify-content:space-between;align-items:flex-start;gap:16px;border-bottom:1px solid #262824;padding-bottom:14px;margin-bottom:12px}.hsm-approved-head h3{font:400 26px/1 Georgia,"Times New Roman",serif;margin:4px 0}.hsm-approved-head p{margin:0;color:#92978f;font-size:10px}.hsm-approved-close{border:1px solid #30332f;background:#151712;color:#fff;width:36px;height:36px;cursor:pointer}.hsm-approved-project{padding:13px 0;border-bottom:1px solid #242622}.hsm-approved-project:last-child{border:0}.hsm-approved-project strong{display:block;font-size:11px}.hsm-approved-project span{display:block;color:#8f948d;font-size:9px;margin-top:4px}.hsm-approved-thumbs{display:flex;gap:5px;margin-top:9px;overflow:auto}.hsm-approved-thumb{width:54px;height:54px;flex:0 0 54px;background:#161813;border:1px solid #292c27;overflow:hidden}.hsm-approved-thumb img{width:100%;height:100%;object-fit:cover}.hsm-approved-empty{padding:34px 8px;text-align:center;color:#868b84;font-size:11px}
  @media(max-width:760px){.hsm-client-bridge{display:block}.hsm-bridge-actions{margin-top:15px;display:grid}.hsm-bridge-btn{width:100%}.hsm-flow-step{min-width:125px}.hsm-client-flow{margin-left:-2px;margin-right:-2px}}
  `;document.head.appendChild(s)}
  function currentStage(){if(document.querySelector('.review-hero'))return 5;if(document.querySelector('.delivery-hero,.delivered-banner'))return 8;if(document.querySelector('.rename-settings-grid,.download-box'))return 7;if(document.querySelector('.cull-card,.smart-cull-hero'))return 3;if(document.querySelector('.photo-grid,.photos-head'))return 2;if(document.querySelector('.table-row,.shot-hub-hero'))return 1;return 0}
  function addFlow(){const tabs=document.querySelector('.tabs');if(!tabs||document.querySelector('.hsm-client-flow'))return;const active=currentStage(),labels=['Shot List','Upload Photos','AI Smart Cull','Photographer Selection','Client Review','Approved for Edit','Lightroom / Photoshop','Final Delivery'];const flow=document.createElement('div');flow.className='hsm-client-flow';flow.setAttribute('aria-label','High Style Match workflow');flow.innerHTML=labels.map((x,i)=>`<div class="hsm-flow-step ${i+1<active?'done':''} ${i+1===active?'on':''}"><i>${String(i+1).padStart(2,'0')}</i><b>${x}</b></div>`).join('');tabs.insertAdjacentElement('afterend',flow)}
  function addReviewBridge(){const hero=document.querySelector('.review-hero');if(!hero||document.querySelector('[data-hsm-client-review-bridge]'))return;const el=document.createElement('section');el.className='hsm-client-bridge';el.dataset.hsmClientReviewBridge='1';el.innerHTML=`<div><div class="kicker">Stage 1 · Client Review</div><h3>Send the photographer shortlist for client approval.</h3><p>Your internal brief review stays separate. The client receives reduced-resolution proofs, enters a six-digit email code, then approves, favourites, rejects or comments. Confirmed photographs move into <b>Approved for Edit</b>.</p></div><div class="hsm-bridge-actions"><button class="hsm-bridge-btn" data-hsm-approved>Approved for Edit</button><button class="hsm-bridge-btn primary" data-hsm-send-review>Send for Client Approval →</button></div>`;hero.insertAdjacentElement('beforebegin',el);el.querySelector('[data-hsm-send-review]').onclick=()=>location.href=ROOT+'invite/';el.querySelector('[data-hsm-approved]').onclick=openApproved}
  function addDeliveryBridge(){const hero=document.querySelector('.delivery-hero,.delivered-banner');if(!hero||document.querySelector('[data-hsm-final-bridge]'))return;const el=document.createElement('section');el.className='hsm-client-bridge';el.dataset.hsmFinalBridge='1';el.innerHTML=`<div><div class="kicker">Stage 2 · Final Delivery</div><h3>Upload edited files and send the finished gallery.</h3><p>This is separate from proof approval. The final client link uses a fresh six-digit verification code and records verification, gallery views, individual downloads and Download All activity.</p></div><div class="hsm-bridge-actions"><button class="hsm-bridge-btn" data-hsm-approved>Approved for Edit</button><button class="hsm-bridge-btn primary" data-hsm-final>Final Upload & Delivery →</button></div>`;hero.insertAdjacentElement('beforebegin',el);el.querySelector('[data-hsm-final]').onclick=()=>location.href=ROOT+'transfer/';el.querySelector('[data-hsm-approved]').onclick=openApproved}
  async function openApproved(){if(document.getElementById('hsmApprovedModal'))return;const back=document.createElement('div');back.id='hsmApprovedModal';back.className='hsm-approved-modal';back.innerHTML=`<div class="hsm-approved-card"><div class="hsm-approved-head"><div><div style="font-size:8px;letter-spacing:.15em;text-transform:uppercase;color:#dfc78f">Photographer Collection</div><h3>Approved for Edit</h3><p>Client-approved photographs ready for Lightroom or Photoshop.</p></div><button class="hsm-approved-close">×</button></div><div id="hsmApprovedBody" class="hsm-approved-empty">Loading approved photographs…</div></div>`;document.body.appendChild(back);back.querySelector('.hsm-approved-close').onclick=()=>back.remove();back.onclick=e=>{if(e.target===back)back.remove()};const body=back.querySelector('#hsmApprovedBody');try{if(!window.HSMCloud?.configured?.())throw new Error('High Style Match cloud is not connected.');const projects=await HSMCloud.listProjects(),groups=[];for(const p of projects){const photos=(await HSMCloud.listPhotos(p.id)).filter(x=>x.status==='approved_for_edit');if(photos.length)groups.push({p,photos})}if(!groups.length){body.className='hsm-approved-empty';body.textContent='No client-approved photographs are waiting for edit yet.';return}body.className='';body.innerHTML='';for(const g of groups){const row=document.createElement('section');row.className='hsm-approved-project';row.innerHTML=`<strong>${esc(g.p.client_name||'Client')} — ${esc(g.p.name)}</strong><span>${g.photos.length} approved photograph${g.photos.length===1?'':'s'}</span><div class="hsm-approved-thumbs"></div>`;body.appendChild(row);const thumbs=row.querySelector('.hsm-approved-thumbs');for(const photo of g.photos.slice(0,12)){const t=document.createElement('div');t.className='hsm-approved-thumb';thumbs.appendChild(t);if(photo.preview_path)HSMCloud.signedPreviewUrl(photo.preview_path,900).then(u=>{if(u)t.innerHTML=`<img src="${u}" alt="">`}).catch(()=>{})}}}catch(e){body.className='hsm-approved-empty';body.textContent=e.message||String(e)}}
  function decorate(){if(decorating)return;decorating=true;requestAnimationFrame(()=>{try{addStyles();addFlow();addReviewBridge();addDeliveryBridge()}finally{decorating=false}})}
  function boot(){decorate();new MutationObserver(()=>decorate()).observe(document.body,{childList:true,subtree:true})}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
