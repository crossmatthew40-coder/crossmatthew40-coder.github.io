name: Publish High Style Match Web

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy publisher disabled. Use the current High Style Match publisher instead."
