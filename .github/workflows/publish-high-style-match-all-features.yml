name: Publish High Style Match Complete Workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-publisher-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy publisher disabled
        run: echo "Legacy complete-build publisher is disabled. Use the current High Style Match delivery publisher for releases."
